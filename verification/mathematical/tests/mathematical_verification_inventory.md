# Master Mathematical & Cryptographic Verification Inventory

This inventory documents all **35+ core mathematical and cryptographic formulas** implemented across the 16 subsystems of the Collaborative Fraud Intelligence Platform. Each formula is cataloged with its LaTeX definition, symbol breakdown, codebase location, practical purpose, and verification test.

---

## 1. Subsystem Formula Inventory

| ID | Subsystem | Formula / Protocol Definition | Codebase Location | Practical System Purpose | Verification Test |
|:---:|:---|:---|:---|:---|:---|
| **M-01** | Federated Learning | $$W_{\text{global}} = \sum_{k=1}^K \frac{n_k}{N} W_k$$ | `fl_engine.py::aggregate_weights` | Weighted parameter aggregation across consortium banks proportional to local dataset sizes. | `test_fl_engine_correctness.py` |
| **M-02** | Federated Learning | $$\min_w \mathcal{L}_k(w) + \frac{\mu}{2} \|w - w^t\|^2$$ | `fl_engine.py::FedProxOptimizer` | Proximal regularization to bound local client update drift under non-IID bank data skew. | `test_fl_engine_hypothesis.py` |
| **M-03** | Federated Learning | $$g_i(w) - c_i + c$$ | `fl_engine.py::SCAFFOLDOptimizer` | Control variates to correct client gradient variance across heterogeneous bank nodes. | `fl_reference_verification.py` |
| **M-04** | Federated Learning | $$\mathcal{L}_{\text{con}} = -\log \frac{\exp(z \cdot z_{\text{glob}} / \tau)}{\exp(z \cdot z_{\text{glob}} / \tau) + \exp(z \cdot z_{\text{prev}} / \tau)}$$ | `fl_engine.py::ModelContrastiveLoss` | Model-contrastive loss aligning local representations with the global model feature space. | `test_fl_engine_hypothesis.py` |
| **M-05** | Federated Learning | $$p_{k,c} \sim \text{Dirichlet}(\alpha \mathbf{p})$$ | `fl_dirichlet_partitioner.py::partition_dataset` | Simulates realistic class imbalance across client financial institutions ($\alpha \in [0.01, 10.0]$). | `test_fl_dirichlet_partitioner.py` |
| **M-06** | Federated Learning | $$s_i = \sum_{r=1}^k \left\lvert \langle \Delta w_i, v_r \rangle \right\rvert^2$$ | `spectral_defense.py::compute_spectral_scores` | Detects backdoor poisoning attacks by measuring parameter update projections onto top singular vectors. | `fl_engine_adversarial_security_evaluation.py` |
| **M-07** | Differential Privacy | $$\bar{g}_i = \frac{g_i}{\max\left(1, \frac{\|g_i\|_2}{C}\right)}$$ | `privacy_service.py::clip_gradients` | Caps individual vector sensitivity to bound single-record influence in differential privacy. | `test_differential_privacy.py` |
| **M-08** | Differential Privacy | $$\tilde{g}_i = \bar{g}_i + \mathcal{N}(0, \sigma^2 C^2 \mathbf{I})$$ | `privacy_service.py::add_noise` | Guarantees $(\epsilon, \delta)$-Differential Privacy for local updates via calibrated Gaussian noise. | `test_differential_privacy.py` |
| **M-09** | Differential Privacy | $$\sigma = \frac{\sqrt{2 \ln(1.25 / \delta)}}{\epsilon}$$ | `label_privacy_guard.py::calculate_sigma` | Derives exact Gaussian noise scale required to satisfy target privacy budget bounds $(\epsilon, \delta)$. | `test_differential_privacy.py` |
| **M-10** | Differential Privacy | $$\text{PSI} = \sum_{i=1}^B (P_i - Q_i) \times \ln\left(\frac{P_i}{Q_i}\right)$$ | `psi_service.py::calculate_psi` | Measures Population Stability Index between baseline and inference feature distributions. | `test_psi_correctness.py` |
| **M-11** | Secure Aggregation | $$y_k = w_k + \sum_{j > k} s_{kj} - \sum_{j < k} s_{jk} \pmod{2^{32}}$$ | `secagg_driver.py::generate_masks` | Zero-sum pairwise seed masking concealing individual bank updates during server aggregation. | `test_secagg_correctness.py` |
| **M-12** | Secure Aggregation | $$K_t = \text{HKDF-SHA256}(\text{seed},\; \text{"secagg-round"} \mathbin{\|} t)$$ | `secagg_driver.py::derive_round_key` | Derives fresh per-round cryptographic keys resisting replay and differencing attacks. | `test_secagg_correctness.py` |
| **M-13** | Secure Aggregation | $$\text{Enc}(m_1) \oplus \text{Enc}(m_2) = \text{Enc}(m_1 + m_2)$$ | `fhe_driver.py::homomorphic_add` | Computes server-side parameter sums over encrypted TenSEAL CKKS ciphertexts without decryption. | `test_fhe_driver.py` |
| **M-14** | Zero-Trust PKI | $$\text{PolicyDecision}(U, R, A, C) = \text{ALLOW} \iff \dots$$ | `abac_engine.py::evaluate_policy` | Attribute-Based Access Control rule evaluation with fail-closed default deny guarantee. | `test_zero_trust_pki_hypothesis.py` |
| **M-15** | Zero-Trust PKI | $$\text{IP}_{\text{uint32}} \mathbin{\&} \text{Mask} == \text{Subnet} \mathbin{\&} \text{Mask}$$ | `abac_engine.py::check_cidr_match` | Verifies client IP address falls strictly within authorized bank VPC CIDR blocks. | `test_zero_trust_pki_robustness.py` |
| **M-16** | Federation Coordinator | $$t_{\text{sleep}} = \text{random.uniform}\left(0, \min\left(15.0, 5.0 \times 2^{\text{attempt}-1}\right)\right)$$ | `client.py::GRPCBankClient._with_retry` | AWS Full-Jitter Exponential Backoff preventing thundering herd reconnection storms. | `test_coordinator_robustness.py` |
| **M-17** | Risk Scoring Engine | $$\text{Risk Score} = \min\left(1000, \max\left(0, \sum w_i S_i \times 1000\right)\right)$$ | `risk_engine.py::compute_composite_score` | Integrates 9 anti-fraud signals into a unified composite risk score in $[0, 1000]$. | `test_risk_engine_correctness.py` |
| **M-18** | Risk Scoring Engine | $$Z = \frac{x - \mu}{\sigma}, \quad S_{\text{amount}} = \frac{1}{1 + e^{-Z}}$$ | `risk_engine.py::normalize_amount` | Sigmoid z-score transformation mapping transaction amounts smoothly to $[0, 1]$. | `test_risk_engine_correctness.py` |
| **M-19** | Graph Intelligence | $$h_v^{(l+1)} = \text{ReLU}\left(W_{\text{self}} h_v^{(l)} + W_{\text{neigh}} \frac{1}{|\mathcal{N}(v)|}\sum h_u^{(l)} + b\right)$$ | `graph_embedding_model.py::GraphSAGELayer` | Inductive 2-hop neighborhood message passing constructing structural entity representations. | `test_graph_intelligence_correctness.py` |
| **M-20** | Graph Intelligence | $$\hat{h}_v = \frac{h_v}{\|h_v\|_2}$$ | `graph_embedding_model.py::GraphSAGELayer` | Projects node embeddings onto unit sphere $\mathbb{S}^{d-1}$ for scale-invariant distance metrics. | `embedding_quality_report.py` |
| **M-21** | Graph Intelligence | $$\text{sim}(u, v) = \frac{h_u \cdot h_v}{\|h_u\|_2 \|h_v\|_2}$$ | `graph_embedding_service.py::compute_cosine_similarity` | Directional cosine similarity measuring topological alignment across bank subgraphs. | `test_graph_intelligence_correctness.py` |
| **M-22** | Model Drift Detection | $$\text{JSD}(P \parallel Q) = \frac{1}{2} D_{\text{KL}}(P \parallel M) + \frac{1}{2} D_{\text{KL}}(Q \parallel M)$$ | `drift_service.py::calculate_jsd` | Symmetric Jensen-Shannon Divergence bounded in $[0, 1]$ to detect feature distribution shifts. | `test_drift_service_correctness.py` |
| **M-23** | Model Drift Detection | $$D = \sup_x |F_1(x) - F_2(x)|$$ | `drift_service.py::calculate_ks_statistic` | Kolmogorov-Smirnov test statistic evaluating continuous feature distribution drift. | `test_drift_service_correctness.py` |
| **M-24** | Explainability (XAI) | $$\phi_i = \sum_{S \subseteq N \setminus \{i\}} \frac{|S|!(|N|-|S|-1)!}{|N|!} (v(S \cup \{i\}) - v(S))$$ | `realtime_explainer.py::compute_shap_values` | Sub-1ms Shapley feature attribution computing exact marginal signal contributions. | `test_explainability_correctness.py` |
| **M-25** | Explainability (XAI) | $$\min_{x'} \|x - x'\|_1 + \lambda (f(x') - y_{\text{target}})^2$$ | `explainability_service.py::generate_counterfactual` | Finds minimal feature modifications needed to reduce transaction risk below block threshold. | `test_explainability_correctness.py` |
| **M-26** | Financial Connectors | $$\text{NormalizedTransaction}(T) = \text{SchemaMap}(\text{XML})$$ | `iso20022_connector.py::parse_pacs008` | Parses ISO 20022 `pacs.008` XML elements into standardized internal transaction schema. | `test_connectors_correctness.py` |
| **M-27** | ETL & Data Pipeline | $$X_{\text{norm}} = \frac{X - \mu}{\sigma}$$ | `data_validator.py::normalize_features` | Standard z-score feature scaling guaranteeing zero mean and unit variance for neural training. | `test_etl_pipeline.py` |
| **M-28** | Smart Contracts Suite | $$\phi_i^{\text{LOO}} = v(N) - v(N \setminus \{i\})$$ | `smart_contract_driver.py` / `ConsortiumIncentiveSettlement.sol` | Leave-One-Out Shapley contribution valuation computed off-chain for fair consortium incentive settlement. | `ConsortiumIncentiveSettlement.test.js` |
| **M-29** | Smart Contracts Suite | $$S_i = \max(0, \lfloor \phi_i^{\text{LOO}} \times 10{,}000 \rfloor)$$ | `ConsortiumIncentiveSettlement.sol::distributeIncentives` | Normalizes Leave-One-Out Shapley scores to non-negative basis points ($10{,}000 = 100\%$). | `test_smart_contracts_hypothesis.py` |
| **M-30** | Smart Contracts Suite | $$\text{Payout}_i = \left\lfloor \text{TotalPoolWei} \times \frac{S_i}{\sum S_k} \right\rfloor$$ | `ConsortiumIncentiveSettlement.sol::distributeIncentives` | Calculates exact CBDC/Stablecoin pool wei allocation for each bank participant. | `test_smart_contracts_hypothesis.py` |
| **M-31** | Audit Logging | $$H_t = \text{SHA-256}(H_{t-1} \mathbin{\|} \text{LogPayload}_t)$$ | `privacy_audit_service.py::append_audit_log` | Cryptographic hash chain guaranteeing tamper-evident immutable audit logs for SOC 2. | `test_audit_logging.py` |
| **M-32** | API Gateway | $$B_{t+1} = \min(B_{\text{capacity}},\; B_t + r \cdot \Delta t) - 1$$ | `main.py::RateLimitMiddleware` | Token bucket rate limiting algorithm preventing API denial-of-service abuse. | `test_api_middleware.py` |
| **M-33** | Telemetry | $$\text{BS} = \frac{1}{N} \sum_{i=1}^N (p_i - y_i)^2$$ | `metrics_service.py::calculate_brier_score` | Brier Score evaluating probability calibration accuracy of global model predictions. | `test_telemetry_metrics.py` |
| **M-34** | Telemetry | $$\text{ECE} = \sum_{m=1}^M \frac{|B_m|}{N} |\text{acc}(B_m) - \text{conf}(B_m)|$$ | `metrics_service.py::calculate_ece` | Expected Calibration Error measuring confidence-accuracy deviation across probability bins. | `test_telemetry_metrics.py` |
| **M-35** | Terraform IaC | $$G = (V, E) \longrightarrow \text{TopologicalSort}(V)$$ | `main.tf` resource DAG solver | Topological sorting of infrastructure dependencies ensuring deadlock-free cloud deployment. | `test_terraform_iac.py` |
